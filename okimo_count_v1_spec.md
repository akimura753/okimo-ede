# OKIMO 人数認識 v1 — 仕様書

ファイル: `okimo_count_v1.html`
公開URL: `https://akimura753.github.io/okimo-ede/okimo_count_v1.html`
作成: 2026年9月9日

## 1. 目的

スマホの背面カメラ映像から視野内の人数をリアルタイムに数え、人数とその左右方向の内訳を音声（およびモールス数字）で使用者に伝える。人接近警告版（`okimo_approach_v2.html`）と同じ配信・記録の枠組みで動作する。

## 2. 動作環境

| 項目 | 内容 |
|---|---|
| 形式 | 単一HTMLファイル（外部読み込みは MediaPipe Tasks Vision のCDNとモデルファイルのみ） |
| 対応 | iPhone Safari（iOS 16.4以降推奨）、Android Chrome |
| 配信 | HTTPS 必須（GitHub Pages） |
| カメラ | 背面 640×480（設定で前面に切替可） |
| 出力 | 音声合成（Web Speech API, ja-JP）、正弦波トーン（Web Audio、骨伝導イヤホン向け）、本体バイブ（Androidのみ） |

## 3. 認識方式

- 検出器: MediaPipe Object Detector（EfficientDet-Lite0 float16）、`person` クラスのみ
- GPUデリゲートを試み、失敗時はCPUに自動切替
- スコアしきい値（既定 0.45）以上の人物ボックスを採用、最大検出人数（既定10）
- 方向: ボックス中心の x 座標で 左（<1/3）／正面／右（>2/3）に分類
- 距離: ボックス高さから概算 `d = f × 身長 / 画素高`（身長既定 1700 mm、f は水平画角65°相当の自動値または設定値）。全身が映る前提の粗い推定であり、「認識する最大距離」（既定 0 = 無制限）で遠方の人を除外する用途に用いる

## 4. 人数の安定化と通知

1. 毎フレームの瞬時人数を「安定化窓」（既定 1.0 秒）の履歴に保持
2. 履歴の最頻値を「安定人数」とする。内訳（左/正面/右）は安定人数と一致する直近フレームの値を採用
3. 安定人数が変化したとき、または（内訳読み上げONで）内訳が変化したときに通知
4. 通知間隔の下限（既定 2.0 秒）内の変化は抑制
5. 定期再通知（既定 0 = しない）を設定すると、人がいる間 N 秒ごとに再読み上げ
6. 0 人になったときの「人はいません」は設定でON/OFF（既定ON）
7. 「今の人数を読む」ボタンでいつでも手動読み上げ

## 5. 伝達内容

| 状況 | 音声 | モールス |
|---|---|---|
| 3人（左1・正面2） | 「3人、左1人、正面2人」 | 3 = ・・・− − |
| 1人（右） | 「右1人」 | 1 = ・− − − − |
| 内訳読み上げOFF | 「3人」 | 同上 |
| 0人 | 「人はいません」 | なし |
| 10人以上 | 「12人、…」 | 桁ごと（1 → 2、桁間3単位） |

モールスは短点:長点 = 1:3、点間 = 短点1個分、桁間 = 短点3個分。短点（既定 120 ms）、音の高さ（既定 700 Hz）、音色（sine / triangle / square、既定 triangle）は設定可。骨伝導イヤホンの省電力による頭切れ対策として、40 Hz の微弱音を常時出力する「Bluetoothを起こしておく」（既定ON）を備える。

## 6. 画面表示

- 上部: 安定人数（大）、瞬時人数、最近接距離
- 映像上: 人物ボックス（左=青、正面=黄、右=赤）と各人の推定距離・スコア、画面3分割線
- 映像下: 左／正面／右の人数
- 下部: セッション経過時間、開始/停止・今の人数を読む・CSVを保存、設定パネル、イベントログ

## 7. 記録（CSV）

- 人を初めて検出した時点からセッション長（既定 30 秒）を1セッションとし、終了時に二重ビープ。次の検出で新セッション開始
- セッション中は毎フレーム1行＋イベント行を記録。UTF-8（BOM付き）、Excel でそのまま開ける
- 「CSVを保存」: iPhone は共有シート（ファイルに保存／AirDrop）、Android はダウンロード
- ファイル名: `okimo_count_YYYYMMDD_HHMMSS.csv`

| 列 | 内容 |
|---|---|
| session_id | セッションID（開始時刻＋連番） |
| timestamp | 端末時刻（ミリ秒） |
| t_session_s | セッション開始からの秒 |
| count_raw | 瞬時人数 |
| count_stable | 安定人数 |
| left / center / right | 内訳 |
| nearest_m | 最近接の推定距離 (m) |
| boxes | 全ボックス `zone:x,y,w,h,score`（0–1正規化、空白区切り） |
| event | `session_start` / `speech:〈読み上げ文〉` / `session_end` |
| morse | 送出したモールス数字 |

`boxes` 列に全検出を残しているため、しきい値・安定化窓・方向境界を変えた再集計が事後に可能。

## 8. 設定一覧（端末に保存、「初期値に戻す」で既定値）

| 設定 | 既定 |
|---|---|
| 検出しきい値 | 0.45 |
| 人数の安定化窓 | 1.0 秒 |
| 変化時の最短通知間隔 | 2.0 秒 |
| 定期再通知 | 0（しない） |
| 認識する最大距離 | 0（無制限） |
| 標準身長 | 1700 mm |
| 焦点距離 | 0（自動） |
| セッション長 | 30 秒 |
| 短点 / 音の高さ / 音色 | 120 ms / 700 Hz / triangle |
| 最大検出人数 | 10 |
| 音声 / 内訳も読む / モールス数字 / 0人も読む | ON / ON / ON / ON |
| 本体バイブ / Bluetoothを起こす / 前面カメラ | ON / ON / OFF |

## 9. 既知の制約

- 距離は全身が映る前提の概算。近距離で下半身が切れると過小評価になる
- 重なった人物は1人に数えられることがある
- iPhone は本体バイブ非対応（骨伝導イヤホンへのトーンで代替）
- 音声合成と同時にモールスを出すため、読み上げ中の音量バランスはイヤホン側で調整する

## 10. 差し替えポイント

検出器は `detectPeople(ts)` 1関数に閉じており、`[{x,y,w,h,s}]`（0–1正規化の人物ボックス配列）を返せば他は変更不要。YOLOv8n を ONNX + onnxruntime-web で動かす場合もこの関数のみ置き換える。
